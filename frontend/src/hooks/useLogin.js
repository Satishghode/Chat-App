
// this is an useLogin method get the data from the server and compare it with the current user data.
const useLogin = () => {
    try {
        
    } catch (error) {
        console.log("this erro for useLOfIN + " + error.message);


    }

}

export default useLogin;